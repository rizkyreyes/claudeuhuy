# Run log

Newest at the bottom. One entry per run: date (Asia/Jakarta), scheduled, produced, spend estimate, problems.

- **2026-09-23** — Batch 1 made interactively (3 videos) and scheduled for 24–26 Sep 7 PM ET on YouTube, TikTok and Instagram (9 posts).
- **2026-09-24** — Daily routine couldn't produce: API keys weren't available in the scheduled-task environment. Pipeline moved to this repo; keys go into the cloud environment's API credentials / environment variables.
- **2026-09-24 (run 2)** — Buffer housekeeping: 9/10 scheduled slots already used by batch 1 (tape-measure-hook, pot-handle-hole, jeans-watch-pocket, 24–26 Sep). The 3 drafts in Buffer are the same batch-1 source uploads (asset ids match the already-scheduled posts) — no new drafts waiting, no queue rows changed. Did not attempt Step 2 (produce): (1) this repo's actual layout is flat — `state/`, `docs/`, `pipeline/`, `examples/`, `media/`, `projects/`, `vendor/` from README.md don't exist, every file landed in the repo root (looks like a non-folder GitHub upload flattened the tree); (2) the environment has no `ffmpeg`/`ffprobe` (checked `which`, `/usr/bin`, `/usr/local/bin` — none) and no `package.json`/`node_modules`, so `npm i gsap`, `npx hyperframes render`, and every b-roll normalize/encode step would fail partway, after already spending fal.ai/ElevenLabs credits on a video that can't be finished. Credential probe results (for next run): ElevenLabs `GET /v1/voices/...` → 200. Pexels `GET /v1/search` → 200. `PIXABAY_KEY` set. fal.ai `queue.fal.run` via `curl` → 200, but via plain `node fetch` → 403; adding `NODE_USE_ENV_PROXY=1` before `node falsync.mjs probe` fixes it (→ 200) — Node isn't picking up the proxy by default in this environment. Needs Rizky: rebuild the repo with real subfolders (or confirm a flat layout and we update ROUTINE.md/README.md to match), and get `ffmpeg`+`ffprobe` plus an `npm install` (gsap, hyperframes) into the environment's setup so a render can actually finish.
