# Run log

Newest at the bottom. One entry per run: date (Asia/Jakarta), scheduled, produced, spend estimate, problems.

- **2026-09-23** — Batch 1 made interactively (3 videos) and scheduled for 24–26 Sep 7 PM ET on YouTube, TikTok and Instagram (9 posts).
- **2026-09-24** — Daily routine couldn't produce: API keys weren't available in the scheduled-task environment. Pipeline moved to this repo; keys go into the cloud environment's API credentials / environment variables.
- **2026-09-25** — First full run of the daily routine.
  - **Repo fix**: the uploaded files were all flat at repo root, not in the `pipeline/`, `state/`, `docs/`, `examples/`, `media/` layout that README.md/ROUTINE.md describe. Reorganized to match (git mv, history preserved); no content changes. Added `.gitignore` (node_modules, render work dirs, intermediate `out.mp4`/`final.mp4`).
  - **Preflight**: `ffmpeg`/`ffprobe` were missing from the environment (only a stripped-down Playwright-bundled ffmpeg with no libx264/aac existed) — installed via `apt-get install ffmpeg`. Node's `fetch` ignores the proxy for `queue.fal.run` (fal probe returned 403) and `api.elevenlabs.io`/`api.pexels.com`/TTS calls — ran those with `NODE_USE_ENV_PROXY=1` as ROUTINE.md's own troubleshooting note suggests, after which fal probed 200 (credential works). Pixabay key present. gsap installed into `pipeline/`.
  - **Step 1**: 6 posts already scheduled (pot-handle-hole Fri 25 Sep, jeans-watch-pocket Sat 26 Sep — 3 platforms each); tape-measure-hook (24 Sep) already sent. All 3 Buffer drafts matched existing queue rows; nothing new to pick up. 4 of 10 free-plan slots open.
  - **Step 2**: produced **pen-cap-hole** (b2-1). Facts re-verified today via web search (Snopes ×2, BS 7272 → ISO 11540, BIC/Astrid Canevet quote, 9 UK child deaths 1970–84) — sources in `projects/pen-cap-hole/publish/sources.txt`. 19 shots: 10 Pexels stills, 5 Pexels clips, 4 AI stills (2 high: hook + "airway" payoff shot, 2 medium: barrel vent, drilling mechanism), 3 AI video clips (minimax h3-max 768P, 6s each, image-to-video from the AI stills — within the "2–3 AI clips" budget). No AI imagery depicts a real/identifiable person or a child; the "breathing" beat uses an adult hand close-up instead of a child's face after an initial stock candidate showed an identifiable child and was rejected. Final: 74.77s, 1080x1920/30fps h264+aac, 26 MB — hyperframes check passed, contact sheet looked clean (captions readable, b-roll on-beat, no black/garbled frames).
  - **Spend estimate**: 2 high-quality + 2 medium-quality AI stills, 3× 6s AI clips at $0.04/s (pre-1 Oct rate) ≈ $0.72 clips + stills ≈ within the $1.1–1.3/video target. Reminder: minimax 768P doubles in price from 1 Oct 2026 — worth testing LTX-2 Fast before then.
  - **Step 2.9**: repo is public, so tried the automatic path — pushed `media/pen-cap-hole.mp4`, confirmed the raw.githubusercontent.com URL (branch `claude/optimistic-ramanujan-vevymc`) serves the exact file, then created the YouTube/TikTok/Instagram posts directly from that URL. Buffer reported `durationMs: 74767` on all three (matches), so all three were created and the queue row marked `scheduled` — no manual upload needed today. Scheduled for Sun 27 Sep 2026, 7 PM ET (next open day; free-plan slot count now 3/10 free after this batch).
